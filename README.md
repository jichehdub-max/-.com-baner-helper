# ITD Banner Redraw Helper

Расширение для точного переноса изображения в область рисования на `itd.com` (включая `canvas.drawing-canvas`).

## Что умеет

- Автонаходит целевой canvas (`800x267` в модалке рисования).
- Показывает live-preview поверх области до применения.
- Вписывание: `cover`, `contain`, `stretch`.
- Точная настройка: масштаб, сдвиг X/Y, прозрачность.
- Режимы точности цвета:
- `Pixel Perfect` (без сглаживания, максимально точные пиксели).
- `Smooth` (мягкое сглаживание).
- `Ctrl+V` на странице: вставка картинки из буфера и авто-применение в canvas.
- Можно загрузить несколько файлов сразу (`multiple`) или вставить несколько картинок из буфера одним `Ctrl+V`.
- AI-генерация текста поста через любой OpenAI-совместимый endpoint (`/v1/chat/completions`) и вставка в строку `Что у вас нового?`.

## Установка

1. Открой Chrome: `chrome://extensions`.
2. Включи `Режим разработчика`.
3. Нажми `Загрузить распакованное расширение`.
4. Выбери папку проекта (где лежит `manifest.json`).

## Как использовать на itd.com

1. Открой окно `Рисование`.
2. Нажми иконку расширения.
3. Нажми `Найти canvas (800x267)`.
4. Выбери режим и параметры.
5. Вариант A: загрузи файл через `Изображение`.
6. Вариант B: на странице нажми `Ctrl+V` (вставка из буфера).
7. Нажми `Применить в canvas` (или включи авто-применение по `Ctrl+V`).

## AI для постов (новое)

1. Открой вкладку `itd.com`, раздел `Посты`.
2. В popup укажи:
   - `AI API Endpoint` (например, endpoint провайдера Megallm в формате chat completions),
   - `AI API Key` (твой персональный ключ),
   - `AI Model`.
3. Введи тему поста в `Тема поста`.
4. Нажми `Сгенерировать и вставить`.
5. После генерации текст автоматически подставится в поле `Что у вас нового?` (`textarea.wall-post-form__textarea`).

### Как получить API ключ MegaLLM

1. Если сайт `megallm.io` не открывается в твоем регионе, зайди через VPN.
2. Перейди на регистрацию: `https://megallm.io/auth/signup` и создай аккаунт.
3. Открой dashboard: `https://megallm.io/dashboard`.
4. В разделе `API Keys` нажми `Create New API Key`.
5. Скопируй ключ формата `sk-mega-...` и вставь его в поле `AI API Key` в плагине.
6. Не публикуй ключ в GitHub и не отправляй его другим людям.

## Примечание

- Если страница масштабирована (zoom браузера), расширение учитывает пересчет CSS->canvas пикселей автоматически.
