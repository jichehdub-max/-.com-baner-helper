# 🎨 ITD Themes - Установка и использование

## Установка расширения

1. Откройте Chrome/Edge и перейдите в `chrome://extensions/`
2. Включите "Режим разработчика" (Developer mode) в правом верхнем углу
3. Нажмите "Загрузить распакованное расширение" (Load unpacked)
4. Выберите папку с расширением

## Автоматическая загрузка тем

Расширение теперь **автоматически загружается** при открытии сайта итд.com!

### Что происходит автоматически:

✅ **При открытии итд.com:**
- Расширение автоматически инжектит панель тем
- Сохранённая тема применяется сразу
- Кнопка 🎨 появляется справа снизу

✅ **При установке/обновлении расширения:**
- Все открытые вкладки с итд.com получают панель автоматически
- Не нужно перезагружать страницы

✅ **При перезагрузке страницы:**
- Тема применяется мгновенно
- Шейдер запускается автоматически (если включен автозапуск)

## Использование

### Панель тем

1. Откройте итд.com
2. Кнопка 🎨 появится справа снизу автоматически
3. Нажмите на неё чтобы открыть панель
4. Выберите тему из списка
5. Включите "Автозапуск темы" чтобы тема применялась при каждом заходе

### Доступные темы

- **Default** - оригинальные цвета сайта (без изменений)
- **Blue** - синяя тема
- **Purple** - фиолетовая тема
- **Cyan** - голубая тема
- **Green** - зелёная тема
- **Red** - красная тема

### Шейдеры

1. Откройте панель тем (кнопка 🎨)
2. Выберите пример шейдера или вставьте свой код
3. Нажмите "Применить шейдер"
4. Включите "Автозапуск шейдера" для автоматического запуска

### Примеры шейдеров

- **Plasma Wave** - плазменные волны
- **Tunnel Effect** - эффект туннеля
- **Color Waves** - цветные волны

## Отладка

Если панель не загружается автоматически:

1. Перезагрузите страницу итд.com (F5)
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте что расширение включено в `chrome://extensions/`
4. Используйте кнопку "🔄 Debug & Reload" в popup для диагностики
5. Попробуйте перезагрузить расширение (кнопка 🔄 в `chrome://extensions/`)

## Логи

Все действия расширения логируются в консоль браузера с префиксом `[ITD Panel]` или `[ITD Background]`.

Откройте консоль (F12) чтобы увидеть:
- Статус загрузки панели
- Применение тем
- Ошибки (если есть)

## Технические детали

### Как работает автозагрузка:

1. **Background Service Worker** (`background.js`) слушает события загрузки страниц
2. При обнаружении итд.com автоматически инжектит CSS и JS
3. Content script (`themes-panel.js`) инициализируется и создаёт UI
4. Сохранённые настройки применяются автоматически

### Файлы расширения:

- `manifest.json` - конфигурация расширения
- `background.js` - service worker для автозагрузки
- `themes/themes-panel.js` - основной скрипт панели тем
- `themes/themes-panel.css` - стили панели и тем
- `popup/popup.html` - интерфейс popup (для отладки)

## Поддержка

Если возникли проблемы:

1. Проверьте что вы на сайте итд.com (xn--d1ah4a.com)
2. Откройте консоль (F12) и найдите сообщения `[ITD Panel]`
3. Попробуйте перезагрузить страницу
4. Используйте кнопку "⚡ Загрузить панель" в popup
5. Проверьте что расширение включено в `chrome://extensions/`
