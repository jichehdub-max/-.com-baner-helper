# 🎨 Руководство по сжатию GIF для итд.com

## Установка

### 1. Установить Python (если еще не установлен)
```bash
# Скачать с https://www.python.org/downloads/
# Или через winget (Windows 11)
winget install Python.Python.3.12
```

### 2. Установить библиотеку Pillow
```bash
pip install Pillow
```

### 3. (Опционально) Установить gifsicle для лучшего сжатия
```bash
# Windows (через Chocolatey)
choco install gifsicle

# macOS
brew install gifsicle

# Linux
sudo apt install gifsicle
```

## Использование

### Базовое сжатие
```bash
python compress-gif.py input.gif output.gif
```

### Сжатие с параметрами
```bash
# Качество 70%, максимум 5 МБ
python compress-gif.py input.gif output.gif --quality 70 --max-size 5

# Уменьшить размер до 800px ширины
python compress-gif.py input.gif output.gif --width 800

# Уменьшить FPS (каждый второй кадр)
python compress-gif.py input.gif output.gif --fps 2

# Комбинация всех параметров
python compress-gif.py input.gif output.gif --quality 70 --width 800 --fps 2 --max-size 5
```

## Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--quality` | Качество (1-100) | 80 |
| `--max-size` | Максимальный размер в МБ | 10 |
| `--width` | Максимальная ширина | - |
| `--height` | Максимальная высота | - |
| `--fps` | Уменьшить FPS (2 = каждый 2-й кадр) | - |
| `--no-gifsicle` | Не использовать gifsicle | false |

## Примеры

### Пример 1: Сжать большой GIF (21 МБ → 5 МБ)
```bash
python compress-gif.py large.gif compressed.gif --quality 70 --max-size 5
```

### Пример 2: Уменьшить размер и FPS
```bash
python compress-gif.py animation.gif small.gif --width 600 --fps 2
```

### Пример 3: Максимальное сжатие
```bash
python compress-gif.py huge.gif tiny.gif --quality 60 --width 500 --fps 3 --max-size 3
```

## Советы по сжатию

### Если GIF слишком большой:

1. **Уменьшить качество** (--quality 60-70)
   - Меньше цветов в палитре
   - Быстрее всего

2. **Уменьшить размер** (--width 600-800)
   - Меньше пикселей = меньше размер
   - Сохраняет плавность

3. **Уменьшить FPS** (--fps 2 или 3)
   - Меньше кадров = меньше размер
   - Может выглядеть менее плавно

4. **Комбинировать все методы**
   ```bash
   python compress-gif.py input.gif output.gif --quality 65 --width 700 --fps 2
   ```

## Результаты сжатия

Типичные результаты:
- **Качество 80%**: сжатие 30-50%
- **Качество 70%**: сжатие 50-70%
- **Качество 60%**: сжатие 70-85%
- **+ уменьшение размера**: дополнительно 50-70%
- **+ уменьшение FPS**: дополнительно 30-50%

## Альтернативные инструменты

### Онлайн сервисы:
- https://ezgif.com/optimize - онлайн оптимизация GIF
- https://gifcompressor.com - простой компрессор
- https://www.iloveimg.com/compress-image/compress-gif - пакетное сжатие

### Десктоп приложения:
- **GIMP** - бесплатный редактор изображений
- **Photoshop** - экспорт с оптимизацией
- **GIF Brewery** (macOS) - специализированный редактор GIF

## Troubleshooting

### Ошибка: "No module named 'PIL'"
```bash
pip install Pillow
```

### Ошибка: "gifsicle not found"
Установите gifsicle или используйте флаг `--no-gifsicle`

### GIF все еще слишком большой
Попробуйте более агрессивные настройки:
```bash
python compress-gif.py input.gif output.gif --quality 50 --width 500 --fps 3
```

### Потеря качества
Увеличьте параметры:
```bash
python compress-gif.py input.gif output.gif --quality 85 --fps 1
```

## Конвертация MP4 в GIF

Если у вас есть MP4 и нужен GIF:

```bash
# Установить ffmpeg
# Windows: choco install ffmpeg
# macOS: brew install ffmpeg
# Linux: sudo apt install ffmpeg

# Конвертировать MP4 в GIF
ffmpeg -i input.mp4 -vf "fps=15,scale=800:-1:flags=lanczos" -c:v gif output.gif

# Затем сжать
python compress-gif.py output.gif compressed.gif --quality 75
```

## Поддержка

Если возникли проблемы:
1. Проверьте что Python установлен: `python --version`
2. Проверьте что Pillow установлен: `pip show Pillow`
3. Попробуйте с флагом `--no-gifsicle`
4. Откройте issue в репозитории проекта
