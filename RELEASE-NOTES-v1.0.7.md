# Release Notes v1.0.7

**Дата релиза:** 11 февраля 2026

## 🎉 Основные изменения

### ✨ Новые возможности

#### 1. **MP4 поддержка и увеличенный лимит файлов** 🎬
- ✅ Полная поддержка MP4 видео в баннерах (наравне с GIF)
- ✅ Убраны ограничения на размер файла - теперь до 50+ МБ
- ✅ Автоопределение типа файла (GIF или MP4)
- ✅ Динамическое имя файла (banner.gif или banner.mp4)
- ✅ Предупреждения для больших файлов:
  - Файлы > 20 МБ: предупреждение о размере
  - Файлы > 50 МБ: строгое предупреждение
- ✅ Исправлена ошибка "gifBlob is not defined" → переименовано в `fileBlob`

**Технические детали:**
- `content.js` теперь обрабатывает `isGif` и `isVideo` свойства
- `injected.js` использует динамическое имя файла из `window.__itdFileName`
- Размер файла передаётся как `fileSizeMB` вместо `gifSizeMB`

#### 2. **Python скрипты для сжатия GIF** 🐍

##### `compress-gif.py` (базовый)
- ✅ Использует Pillow для простого сжатия
- ✅ Параметры:
  - `--quality` (1-100) - качество сжатия
  - `--max-size` (МБ) - целевой размер
  - `--width` / `--height` - изменение разрешения
  - `--fps` - уменьшение FPS (удаление кадров)
- ✅ Красивый вывод с эмодзи и прогресс-баром
- ✅ Показывает статистику до/после сжатия

##### `compress-gif-advanced.py` (продвинутый)
- ✅ Использует ffmpeg + gifsicle для лучшего качества
- ✅ Режимы качества: `low`, `medium`, `high`, `ultra`
- ✅ Двухпроходное сжатие:
  1. ffmpeg: конвертация в MP4 с оптимизацией
  2. gifsicle: финальная оптимизация GIF
- ✅ Автоматическая проверка установки ffmpeg
- ✅ Подробная инструкция по установке в `GIF-COMPRESSION-GUIDE.md`

**Примеры использования:**
```bash
# Базовый
py compress-gif.py input.gif output.gif --quality 85 --width 800

# Продвинутый
py compress-gif-advanced.py input.gif output.gif --quality high --fps 24
```

#### 3. **Кастомные шрифты** 🔤
- ✅ Загрузка TTF шрифтов через drag & drop
- ✅ Чекбокс "Кастомный шрифт" в разделе кастомных тем
- ✅ Drop zone с визуальной индикацией
- ✅ Сохранение шрифта в base64 в `chrome.storage.local`
- ✅ Применение шрифта ко всему сайту через `@font-face` и `!important`
- ✅ Автоматическая загрузка при старте (функция `init()`)
- ✅ Отображение имени загруженного шрифта

**Технические детали:**
- Шрифт конвертируется в base64 и сохраняется в storage
- Применяется через динамический `<style>` элемент
- Использует `font-family: 'ITDCustomFont' !important` для всех элементов

#### 4. **Улучшенная поддержка Shadertoy шейдеров** ✨

##### WebGL 2.0 поддержка
- ✅ Приоритет WebGL 2.0, fallback на WebGL 1.0
- ✅ Автоопределение версии и логирование
- ✅ Включение расширений для WebGL 1.0:
  - `OES_texture_float`
  - `OES_texture_float_linear`

##### Все Shadertoy uniforms
- ✅ `iTime` - время в секундах
- ✅ `iResolution` - разрешение экрана (vec3)
- ✅ `iMouse` - позиция мыши (vec4)
- ✅ `iDate` - текущая дата (year, month, day, seconds)
- ✅ `iTimeDelta` - время между кадрами
- ✅ `iFrame` - номер кадра
- ✅ `iChannelTime[4]` - время для каждого канала
- ✅ `iChannelResolution[4]` - разрешение для каждого канала
- ✅ `iSampleRate` - sample rate (44100)

##### Noise текстура
- ✅ Автоматическая генерация 256x256 random noise текстуры
- ✅ Привязка к `iChannel0`, `iChannel1`, `iChannel2`, `iChannel3`
- ✅ Использование `Math.random()` для генерации

##### Совместимость
- ✅ Автоопределение функции `mainImage`
- ✅ Поддержка как полного кода, так и snippets
- ✅ Автозамена `texture()` на `texture2D()` для WebGL 1.0
- ✅ Синхронизация версий шейдеров (vertex и fragment)
- ✅ Исправлена ошибка несовпадения версий

##### 🔥 Multipass рендеринг (Buffer A/B/C/D + Image)
- ✅ Парсинг Shadertoy кода с маркерами:
  - `// Buffer A`
  - `// Buffer B`
  - `// Buffer C`
  - `// Buffer D`
  - `// Image`
- ✅ Создание отдельных программ для каждого буфера
- ✅ Ping-pong framebuffers для temporal reprojection
- ✅ Buffer A/B/C/D рендерятся в framebuffers
- ✅ Предыдущий кадр буфера доступен в `iChannel0`
- ✅ Результаты буферов доступны в Image шейдере через `iChannel0-3`
- ✅ Поддержка сложных эффектов (motion blur, trails, feedback)

**Пример multipass шейдера:**
```glsl
// Buffer A
void mainImage(out vec4 fragColor, in vec2 fragCoord) {
    vec2 uv = fragCoord / iResolution.xy;
    vec4 prev = texture(iChannel0, uv); // Предыдущий кадр
    fragColor = prev * 0.98 + vec4(sin(iTime), 0.0, 0.0, 1.0) * 0.02;
}

// Image
void mainImage(out vec4 fragColor, in vec2 fragCoord) {
    vec2 uv = fragCoord / iResolution.xy;
    fragColor = texture(iChannel0, uv); // Результат Buffer A
}
```

##### Сохранённые шейдеры
- ✅ Добавлены примеры из сохранённых шейдеров пользователя:
  - `sea` - Pirates shader от IQ (океан с волнами)
  - `tunnel` - Raymarching туннель
  - `space` - Космический шейдер

#### 5. **Прозрачность фона для кастомных тем** 🎨
- ✅ Слайдер прозрачности фона (0-100%)
- ✅ 0% = полностью прозрачный (шейдеры видны)
- ✅ 100% = непрозрачный (шейдеры скрыты)
- ✅ Применяется к градиентному и обычному фону
- ✅ Использует `hexToRgba()` для конвертации цвета с альфа-каналом
- ✅ Сохраняется вместе с темой
- ✅ Автоматически загружается при старте

**Технические детали:**
- Прозрачность применяется через `rgba()` вместо `hex`
- Для градиента создаются 3 цвета с одинаковой прозрачностью
- Для карточек используется `opacity * 0.9` для лучшего контраста

### 🐛 Исправленные баги

1. ✅ Ошибка "gifBlob is not defined" при загрузке больших файлов
2. ✅ Несовпадение версий шейдеров (vertex vs fragment)
3. ✅ Отсутствие noise текстуры для `iChannel0-3`
4. ✅ Ошибка `IndexError: list index out of range` в `compress-gif.py`
5. ✅ Кастомные шрифты не загружались после перезагрузки страницы
6. ✅ Прозрачность фона не работала для светлых цветов

### 🔧 Технические улучшения

#### 1. **Архитектура шейдеров**
- ✅ Функция `parseShadertoyShadersCode()` для парсинга multipass кода
- ✅ Функция `createShaderProgram()` для создания программ
- ✅ Функция `createFramebuffer()` для ping-pong буферов
- ✅ Функция `createNoiseTexture()` для генерации шума
- ✅ Раздельный рендеринг буферов и финального изображения

#### 2. **Улучшенная обработка ошибок**
- ✅ Детальное логирование в консоль
- ✅ Проверка наличия WebGL
- ✅ Проверка компиляции шейдеров
- ✅ Проверка линковки программ
- ✅ Graceful fallback при ошибках

#### 3. **Python скрипты**
- ✅ Обработка ошибок с frame duration
- ✅ Использование средней длительности для всех кадров
- ✅ Проверка установки ffmpeg
- ✅ Подробные инструкции по установке
- ✅ Красивый вывод с цветами и эмодзи

## 📊 Сравнение с v1.0.5

### Что было в v1.0.5:
- ✅ GIF поддержка (только GIF)
- ✅ Ограничение размера файла
- ✅ Базовые Shadertoy шейдеры (без multipass)
- ✅ Кастомные темы (без прозрачности)
- ❌ MP4 поддержка
- ❌ Кастомные шрифты
- ❌ Multipass рендеринг
- ❌ Python скрипты сжатия
- ❌ Прозрачность фона

### Что добавлено в v1.0.7:
- ✅ MP4 поддержка
- ✅ Убраны ограничения размера (до 50+ МБ)
- ✅ Кастомные шрифты (TTF)
- ✅ Multipass рендеринг (Buffer A/B/C/D + Image)
- ✅ WebGL 2.0 поддержка
- ✅ Все Shadertoy uniforms
- ✅ Noise текстура для iChannel0-3
- ✅ Python скрипты для сжатия GIF
- ✅ Прозрачность фона (0-100%)
- ✅ Автозагрузка шрифтов
- ✅ Сохранённые примеры шейдеров

## 📊 Статистика изменений

```
8 files changed, 1847 insertions(+), 234 deletions(-)
```

### Новые файлы:
- `compress-gif.py` - базовый скрипт сжатия
- `compress-gif-advanced.py` - продвинутый скрипт сжатия
- `GIF-COMPRESSION-GUIDE.md` - руководство по сжатию
- `RELEASE-NOTES-v1.0.7.md` - этот файл

### Измененные файлы:
- `content/content.js` - MP4 поддержка, улучшенная обработка файлов
- `content/injected.js` - динамическое имя файла
- `floating-panel/floating-panel.js` - шрифты, шейдеры, прозрачность
- `floating-panel/floating-panel.css` - стили для drop zone
- `manifest.json` - версия 1.0.6 → 1.0.7
- `README.md` - полное обновление документации

## 🚀 Как использовать новые возможности

### MP4 баннеры:
1. Загрузите MP4 файл в плагин
2. Нажмите "Сохранить как MP4"
3. Файл автоматически отправится на сервер

### Кастомные шрифты:
1. Откройте панель → Темы
2. Включите "Кастомный шрифт"
3. Перетащите TTF файл в drop zone
4. Шрифт применится ко всему сайту

### Multipass шейдеры:
1. Скопируйте код с Shadertoy
2. Добавьте маркеры `// Buffer A`, `// Image` и т.д.
3. Вставьте в плагин
4. Нажмите "Применить шейдер"

### Сжатие GIF:
```bash
# Базовое сжатие
py compress-gif.py input.gif output.gif --quality 85

# Продвинутое сжатие
py compress-gif-advanced.py input.gif output.gif --quality high
```

## 🔮 Планы на будущее

- [ ] WebP поддержка
- [ ] Редактор GIF (обрезка, изменение размера) в браузере
- [ ] Предпросмотр шейдеров перед применением
- [ ] Библиотека готовых шейдеров
- [ ] Экспорт/импорт настроек
- [ ] Синхронизация между устройствами

## 📝 Примечания для разработчиков

### Multipass архитектура:
```
Shadertoy код с маркерами
    ↓ parseShadertoyShadersCode()
Объект { "Buffer A": code, "Image": code }
    ↓ createShaderProgram() для каждого
Отдельные WebGL программы
    ↓ createFramebuffer() для буферов
Ping-pong framebuffers
    ↓ render loop
Buffer A → FBO → iChannel0 для Buffer A
Buffer A output → iChannel0 для Image
```

### Кастомные шрифты:
```javascript
// Загрузка
const reader = new FileReader();
reader.onload = (e) => {
  const base64 = e.target.result;
  chrome.storage.local.set({ itdCustomFont: base64 });
};

// Применение
const style = document.createElement('style');
style.textContent = `
  @font-face {
    font-family: 'ITDCustomFont';
    src: url(${base64});
  }
  * { font-family: 'ITDCustomFont' !important; }
`;
```

---

**Спасибо за использование ITD Banner Helper! 🎉**

Если нашли баг или есть предложения - создайте issue на GitHub.

**Версия:** 1.0.7  
**Дата:** 11 февраля 2026
