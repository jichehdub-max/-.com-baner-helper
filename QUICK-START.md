# 🚀 Быстрый старт - Темы и шейдеры для итд.com

## Установка

1. Откройте `chrome://extensions`
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку с расширением
5. Нажмите кнопку обновления (🔄) если расширение уже было установлено

## Первый запуск

1. **Откройте итд.com** (любую страницу)
2. **Откройте консоль** (F12)
3. **Проверьте логи:**
   ```
   [ITD Themes] Initializing theme system...
   [ITD Themes] Active theme: default
   [ITD Themes] Theme applied successfully to div.layout
   [ITD Themes] Theme system loaded successfully
   ```

4. **Откройте popup расширения** (клик на иконку)
5. **Прокрутите вниз** до секций "🎨 Кастомные темы" и "✨ Shadertoy шейдеры"

## Смена темы

1. В popup найдите выпадающий список "Активная тема"
2. Выберите тему:
   - **Default** - сине-голубая (по умолчанию)
   - **Dark Purple** - темно-фиолетовая
   - **Neon Cyan** - неоново-циановая
3. Тема применится мгновенно к `div.layout` на странице

## Создание своей темы

1. В popup раскройте "Создать новую тему"
2. Введите название (например: "My Theme")
3. Выберите цвета:
   - **Primary Color** - основной цвет
   - **Secondary Color** - дополнительный цвет
   - **Text Color** - цвет текста
4. Нажмите "Создать тему"
5. Тема автоматически применится

## Применение шейдера

### Из примеров:
1. В popup выберите пример из списка (например: "Plasma Wave")
2. Нажмите "Применить шейдер"
3. Анимированный фон появится поверх страницы

### Из Shadertoy:
1. Откройте [shadertoy.com](https://www.shadertoy.com/)
2. Найдите понравившийся шейдер
3. Скопируйте весь код из редактора (функцию `mainImage`)
4. В popup вставьте код в текстовое поле
5. Нажмите "Применить шейдер"

## Что изменяется

### Темы влияют на:
- `div.layout` - основной контейнер сайта (фоновые градиенты)
- Элементы расширения (рамки, бейджи, уведомления)
- Декоративные радиальные градиенты

### Шейдеры:
- Создают fullscreen canvas поверх страницы
- Рендерятся через WebGL
- Имеют прозрачность 30% и blend mode `screen`
- Не блокируют взаимодействие с сайтом

## Тестирование

Откройте `test-page.html` в браузере с установленным расширением:
1. Проверьте что система загружена
2. Переключайте темы кнопками
3. Применяйте шейдеры
4. Смотрите логи в консоли

## Отладка

### Темы не применяются:
```javascript
// В консоли проверьте:
window.itdThemeSystem.initialized  // должно быть true
window.itdThemeSystem.activeTheme  // текущая тема
document.getElementById('itd-custom-theme-style')  // стили должны быть
```

### Шейдер не работает:
```javascript
// В консоли проверьте:
document.getElementById('itd-shader-canvas')  // canvas должен быть
window.itdThemeSystem.shader  // код шейдера
```

### Проверка WebGL:
Откройте https://get.webgl.org/ - должна быть зеленая галочка

## Горячие клавиши

Нет встроенных, но можно добавить через расширение или создать свои в коде.

## Сохранение

Все настройки автоматически сохраняются в `chrome.storage.local`:
- Кастомные темы
- Активная тема  
- Код активного шейдера

Данные сохраняются между сессиями браузера.

## Примеры использования

### Минималистичная тема:
```
Название: Minimal Dark
Primary: #333333
Secondary: #222222
Text: #ffffff
```

### Яркая тема:
```
Название: Bright Neon
Primary: #ff00ff
Secondary: #00ffff
Text: #ffffff
```

### Простой градиент шейдер:
```glsl
void mainImage(out vec4 fragColor, in vec2 fragCoord) {
  vec2 uv = fragCoord / iResolution.xy;
  vec3 col = vec3(uv.x, uv.y, 0.5);
  fragColor = vec4(col, 1.0);
}
```

## Поддержка

Если что-то не работает:
1. Проверьте консоль (F12) на ошибки
2. Перезагрузите расширение
3. Перезагрузите страницу итд.com
4. Проверьте что используете актуальную версию Chrome/Edge

## Версия

Текущая версия: **1.0.4**

Changelog: см. `CHANGELOG.md`
